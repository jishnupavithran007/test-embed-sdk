{
  "installCommand": "npm install -g npm@11 && rm -f package-lock.json && npm install --no-audit --no-fund || (echo '=================== NPM DEBUG LOG ===================' && cat /vercel/.npm/_logs/*-debug-0.log && exit 1)"
}
